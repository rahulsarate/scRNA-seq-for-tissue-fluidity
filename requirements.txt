# scRNA-seq Wound Healing — Python Requirements
# ================================================
# Use: pip install -r requirements.txt
# Or:  conda env create -f configs/conda_envs/scrna_wound_healing.yml
# Python 3.10+ required (tested with 3.10.11)
# ================================================

# Core data science
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# scRNA-seq analysis
scanpy>=1.9
anndata>=0.10
leidenalg
igraph

# Visualization
matplotlib>=3.7
seaborn>=0.12

# File formats
h5py

# Enrichment analysis
gseapy>=1.0

# Utilities
pyyaml

# Optional — install via conda on Windows (require C++ build tools for pip):
# harmonypy          # needs torch → use conda
# bbknn              # needs annoy (C++) → use conda
# scrublet           # needs annoy (C++) → use conda
# decoupler>=1.5     # needs marsilea → large dependency tree
# tables             # HDF5 tables → use conda
# scvelo>=0.3        # needs annoy (C++) → use conda
# cellrank>=2.0      # needs scvelo → use conda
